<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1, user-scalable=no">
  <title>Data Barang</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Inter', sans-serif;
    }

    body {
      background: #f4f6f8;
      color: #333;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
      padding: 10px;
    }

    h1 {
      font-size: 1.3rem;
      font-weight: 600;
      margin: 15px 0;
      color: #222;
      text-align: center;
    }

    .table-container {
      width: 100%;
      max-width: 600px;
      background: #fff;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      overflow: hidden;
    }

    table {
      width: 100%;
      border-collapse: collapse;
    }

    thead {
      background: #007bff;
      color: #fff;
    }

    th, td {
      text-align: left;
      padding: 10px 12px;
      font-size: 0.95rem;
    }

    tr:nth-child(even) {
      background: #f9f9f9;
    }

    @media (max-width: 600px) {
      th, td {
        padding: 8px;
        font-size: 0.85rem;
      }

      h1 {
        font-size: 1.1rem;
      }

      .table-container {
        border-radius: 8px;
      }
    }

    .loading {
      text-align: center;
      padding: 20px;
      color: #555;
    }
  </style>
</head>
<body>

  <h1>📦 Daftar Barang</h1>

  <div class="table-container">
    <table id="dataTable">
      <thead>
        <tr>
          <th>Nama</th>
          <th>Item</th>
          <th>Qty</th>
        </tr>
      </thead>
      <tbody id="tableBody">
        <tr><td colspan="3" class="loading">Memuat data...</td></tr>
      </tbody>
    </table>
  </div>

  <script>
    async function loadData() {
      const url = "https://api.sheetbest.com/sheets/498acd7a-ed95-4b05-8700-de1d79567933";
      const tbody = document.getElementById("tableBody");

      try {
        const res = await fetch(url);
        const data = await res.json();
        tbody.innerHTML = "";

        if (data.length === 0) {
          tbody.innerHTML = "<tr><td colspan='3' style='text-align:center;'>Tidak ada data</td></tr>";
          return;
        }

        data.forEach(row => {
          const tr = document.createElement("tr");
          tr.innerHTML = `
            <td>${row.Nama || '-'}</td>
            <td>${row.Item || '-'}</td>
            <td>${row.Qty || '-'}</td>
          `;
          tbody.appendChild(tr);
        });
      } catch (error) {
        tbody.innerHTML = "<tr><td colspan='3' style='color:red; text-align:center;'>Gagal memuat data</td></tr>";
        console.error("Error:", error);
      }
    }

    loadData();
  </script>

</body>
</html>
